export default () => ({
  root: {
    top: 0,
    left: 0,
    zIndex: 10,
    width: "100vw",
    height: "100vh",
    display: "flex",
    position: "absolute",
    alignItems: "center",
    justifyContent: "center"
  }
});
